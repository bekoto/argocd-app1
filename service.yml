apiVersion: v1
kind: Service
metadata:
  name: bb-app1-service
  labels:
    app: bb-app1
spec:    
  selector:
    app: bb-app1
  type: NodePort
  ports:
    - protocol: TCP
      port: 8087
      targetPort: 80
      nodePort: 30007